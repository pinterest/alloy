import { refkey } from "@alloy-js/core";

/**
 * Refkey for the canonical `PageInfo` type.
 * Kept in builtins to provide a shared identity without pulling in JSX helpers.
 */
export const PageInfo = refkey("PageInfo");
